import type { TextAreaRef } from 'rc-textarea';
type ResizeWrapperHandler = (rcTextArea: TextAreaRef | null) => void;
declare const useHandleResizeWrapper: () => ResizeWrapperHandler;
export default useHandleResizeWrapper;
